# Supabase Configuration TODO

## Completed:
- [x] Created `.env.local` with placeholders for `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`.

## Remaining:
- [ ] Replace placeholders in `.env.local` with real Supabase values (get from https://supabase.com/dashboard > project > Settings > API).
- [ ] Verify env warning disappears and test Supabase features (e.g., sign-in, dashboard).
- [ ] Get your Supabase project URL and anon key from https://supabase.com/dashboard > your project > Settings > API.
- [ ] Replace placeholders in `.env.local` with real values.
- [x] Restart dev server: `npm run dev`
- [ ] Replace placeholders in `.env.local` with real Supabase values (get from dashboard).
